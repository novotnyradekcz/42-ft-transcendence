# 42-ft-transcendence

Practicing with react/typescript frontend, pushed without node_modules
to setup:
- `npm install` - installs node_modules
- `npm run build` - builds it
- `npm run lint` - linter for format check
- `npm run dev` - starts it
