# This file is a placeholder. The active nginx configuration is selected at
# container startup by docker-entrypoint.sh based on the ENVIRONMENT variable:
#   ENVIRONMENT=local      -> nginx.local.conf      (self-signed certificate)
#   ENVIRONMENT=production -> nginx.production.conf (Let's Encrypt certificate)
